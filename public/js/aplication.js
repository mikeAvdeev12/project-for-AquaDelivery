let form = document.getElementById('inn-form');
console.log(form)

form.addEventListener('submit', async (event) => {
  event.preventDefault();

  let answer = document.getElementById('inn-number').nodeValue;

  
})